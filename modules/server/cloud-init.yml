#cloud-config
package_update: true
package_upgrade: true

packages:
  - fail2ban
  - ufw
  - curl
  - git

users:
  - name: ${ssh_user}
    groups: [sudo]
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ${ssh_pub_key}

write_files:
  - path: /etc/ssh/sshd_config.d/99-hardening.conf
    content: |
      ${indent(6, sshd_config)}
  - path: /etc/fail2ban/jail.local
    content: |
      ${indent(6, fail2ban_config)}

runcmd:
  # SSH hardening
  - rm -f /etc/ssh/sshd_config.d/50-cloud-init.conf
  - systemctl disable --now ssh.socket || true
  - systemctl enable ssh.service
  - systemctl restart ssh.service
  - systemctl enable fail2ban
  - systemctl restart fail2ban

  # Firewall
  - ufw default deny incoming
  - ufw default allow outgoing
  - ufw allow ${ssh_port}/tcp
  - ufw --force enable

  # Install k3s (without Traefik — we use Cloudflare Tunnel)
  - curl -sfL https://get.k3s.io | INSTALL_K3S_EXEC="--disable=traefik --write-kubeconfig-mode=644" sh -

  # Wait for k3s to be ready
  - |
    for i in $(seq 1 60); do
      /usr/local/bin/kubectl get nodes 2>/dev/null && break
      sleep 5
    done

  # Install cloudflared
  - curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o /tmp/cloudflared.deb
  - dpkg -i /tmp/cloudflared.deb
  - rm /tmp/cloudflared.deb
  - cloudflared service install ${tunnel_token}
  - systemctl enable cloudflared
  - systemctl start cloudflared

  # Build OpenClaw image with nerdctl
  - |
    git clone --depth 1 --branch ${openclaw_version} https://github.com/openclaw/openclaw.git /tmp/openclaw-build
    cd /tmp/openclaw-build
    /usr/local/bin/nerdctl --namespace k8s.io build -t localhost/openclaw:latest .
    rm -rf /tmp/openclaw-build

  # Build fcm-push image
  - mkdir -p /tmp/fcm-push-build
  - |
    cat > /tmp/fcm-push-build/Dockerfile <<'DOCKERFILE'
    FROM node:22-alpine
    WORKDIR /app
    COPY index.js .
    CMD ["node", "index.js"]
    DOCKERFILE
  - |
    cat > /tmp/fcm-push-build/index.js <<'INDEXJS'
    ${indent(4, fcm_push_index_js)}
    INDEXJS
  - |
    cd /tmp/fcm-push-build
    /usr/local/bin/nerdctl --namespace k8s.io build -t localhost/fcm-push:latest .
    rm -rf /tmp/fcm-push-build

  # Create data directories
  - mkdir -p /opt/openclaw-data/{openclaw,n8n,fcm-push}
  - chown -R 1000:1000 /opt/openclaw-data

  # Signal provisioning complete
  - touch /opt/openclaw-ready
