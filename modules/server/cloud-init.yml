#cloud-config
package_update: true
package_upgrade: true

packages:
  - docker.io
  - docker-compose-v2
  - fail2ban
  - ufw
  - curl

users:
  - name: ${ssh_user}
    groups: [sudo, docker]
    shell: /bin/bash
    sudo: ALL=(ALL) NOPASSWD:ALL
    ssh_authorized_keys:
      - ${ssh_pub_key}

write_files:
  - path: /etc/ssh/sshd_config.d/99-hardening.conf
    content: |
      ${indent(6, sshd_config)}
  - path: /etc/fail2ban/jail.local
    content: |
      ${indent(6, fail2ban_config)}
  - path: /opt/openclaw/docker-compose.yml
    content: |
      ${indent(6, docker_compose)}

runcmd:
  - systemctl enable docker
  - systemctl start docker

  - ufw default deny incoming
  - ufw default allow outgoing
  - ufw allow ${ssh_port}/tcp
  - ufw --force enable

  - rm -f /etc/ssh/sshd_config.d/50-cloud-init.conf
  - systemctl disable --now ssh.socket || true
  - systemctl enable ssh.service
  - systemctl restart ssh.service

  - systemctl enable fail2ban
  - systemctl restart fail2ban

  - curl -fsSL https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64.deb -o /tmp/cloudflared.deb
  - dpkg -i /tmp/cloudflared.deb
  - rm /tmp/cloudflared.deb

  - cloudflared service install ${tunnel_token}
  - systemctl enable cloudflared
  - systemctl start cloudflared

  - chown ${ssh_user}:docker /opt/openclaw
  - chown ${ssh_user}:docker /opt/openclaw/docker-compose.yml
  - touch /opt/openclaw/.env
  - chown ${ssh_user}:docker /opt/openclaw/.env
  - cd /opt/openclaw && docker compose --env-file /opt/openclaw/.env up -d
